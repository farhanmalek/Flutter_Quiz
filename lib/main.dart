import "package:flutter/material.dart";
import "package:second_app/quiz.dart";

//every app starts like this, w/ main function and runApp from the material package.
// We are using a Quiz(widget or class) think of it as like the main component or parent widget.
void main() {
  runApp(
   const Quiz(),
    
  );
}
